# Tunnex CLI packages

Official Tunnex Linux package distribution infrastructure.

Repository publication is being validated. No APT/RPM/APK/pacman installation
endpoint is live yet. The public signing keys are in `keys/`.

Existing CLI binaries: https://github.com/tunnexio/tunnex/releases/latest

Homebrew tap: https://github.com/tunnexio/homebrew-tap
